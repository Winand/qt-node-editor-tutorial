[tool.poetry]
name = "opencv-flow"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"
packages = [{include = "opencv_flow", from = "src"}]

[tool.poetry.dependencies]
python = "^3.10"
qtpy = "^2.4.1"
pyqt6 = "^6.5.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
